<x-layouts.app title="Banking Engine"><section class="max-w-7xl mx-auto px-6 py-20"><div class="max-w-3xl"><p class="text-sky-400 font-medium">Dynamic Banking Intelligence</p><h1 class="text-5xl md:text-7xl font-semibold tracking-tight mt-4">Hitung banking tanpa menghafal rumus.</h1><p class="text-slate-400 text-lg mt-6">Kalkulator, parameter, versi, dan aturan dibaca dari database—bukan hard-coded di halaman.</p></div><div class="mt-16 space-y-12">@foreach($segments as $segment)<section><div class="flex items-end justify-between mb-5"><div><p class="text-sm text-slate-500">Segment</p><h2 class="text-2xl font-semibold">{{ $segment->name }}</h2></div></div><div class="grid md:grid-cols-2 xl:grid-cols-3 gap-4">@foreach($segment->categories as $category) @foreach($category->calculators as $calculator)<a href="{{ route('calculator.show',$calculator->slug) }}" class="rounded-2xl border border-white/10 bg-white/[.04] p-6 hover:bg-white/[.07] transition"><p class="text-xs uppercase tracking-widest text-slate-500">{{ $category->name }}</p><h3 class="mt-2 text-xl font-medium">{{ $calculator->name }}</h3><p class="mt-3 text-sm text-slate-400">{{ $calculator->short_description }}</p></a>@endforeach @endforeach</div></section>@endforeach</div></section></x-layouts.app>
